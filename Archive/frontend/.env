// In your React app, add this to debug environment variables
// Add this to your main component or App.js temporarily

console.log('All env variables:', process.env);
console.log('API URL:', process.env.REACT_APP_API_URL);
console.log('Node ENV:', process.env.NODE_ENV);

// For Vercel, make sure your env variable is named correctly:
// REACT_APP_API_URL=https://archive-mi73.onrender.com

// In your API calls, use it like this:
const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000';

console.log('Using API Base URL:', API_BASE_URL);

// Example API call
const fetchSemesters = async () => {
  try {
    console.log('Fetching from:', `${API_BASE_URL}/api/semesters`);
    const response = await fetch(`${API_BASE_URL}/api/semesters`);
    
    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }
    
    const data = await response.json();
    console.log('API Response:', data);
    return data;
  } catch (error) {
    console.error('API Error:', error);
    throw error;
  }
};